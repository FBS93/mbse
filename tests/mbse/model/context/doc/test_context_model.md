# Context Model Test

This test file validates the shared context model fixture as authored data.

- Confirms the JSON fixture loads and validates against the context schema.
- Confirms structural queries return the expected enums and variables.
- Confirms unknown enum and variable lookups fail explicitly instead of returning ambiguous results.
