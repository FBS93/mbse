# Generate edf_config.h based on edf_config.h.in
configure_file(edf_config.h.in ${CMAKE_CURRENT_BINARY_DIR}/inc/edf_config.h @ONLY)

add_library(edf STATIC "")
target_include_directories(edf
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/inc
    PUBLIC ${CMAKE_CURRENT_BINARY_DIR}/inc) # Required to find autogenerated edf_config.h file

add_subdirectory(src)
add_subdirectory(edf_test)
add_subdirectory(examples)

if(ECF_TEST)
    add_subdirectory(test)
endif()
